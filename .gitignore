# Dependencies and build output
node_modules/
dist/

# Local secrets
.env
.env.local
.env.*.local

# Legacy snapshot (kept on disk, not for the public repo)
legacy/

# Private banks, scores, reference PDFs, and one-off scripts.
# Anchored so public/sample/* stays publishable.
/local/

# Backup JSON dumps
*backup*.json
